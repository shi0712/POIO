import { Rocket } from 'lucide-react';import art from '../assets/games/crash-cover.png';import { defineDesktopGame } from './types';
export const crashDesktopPlugin=defineDesktopGame({id:'crash',name:'Crash',eyebrow:'CRASH',description:'火箭升空后及时结算，与社区成员共享同一轮。',accent:'#d5ff58',art,icon:Rocket});
