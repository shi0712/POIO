Windows 版新增完整的后台运行和桌面行为设置。

- 新增 POIO 系统托盘，可打开窗口、静音、断开语音、检查更新和退出
- 关闭主窗口时可最小化到托盘，语音和后台下载不会中断
- 首次关闭到托盘时会显示明确提示，避免误以为程序已经退出
- 设置中可关闭“最小化到托盘”，恢复直接退出行为
- 支持登录 Windows 后自动在托盘后台启动
- 重复启动 POIO 时会唤醒已有窗口，不再出现多个客户端实例
- 包含 0.3.9 的主动更新弹窗、更新说明和下载进度
