## POIO 0.4.0 Preview

本版本完善 Windows 桌面生命周期体验：

- 新增 POIO 系统托盘；
- 托盘菜单支持打开主窗口、切换麦克风静音、断开语音、检查更新和退出；
- 关闭主窗口默认最小化到托盘，语音和后台下载保持运行；
- 首次关闭到托盘会显示系统提示；
- 设置中可以关闭“最小化到托盘”，恢复直接退出行为；
- 支持登录 Windows 后在托盘后台自动启动；
- 重复启动 POIO 时会唤醒已有窗口，不再启动多个实例；
- 包含 0.3.9 的主动更新弹窗、更新说明和实时下载进度。

安装包：

`POIO-0.4.0-x64-Setup.exe`
