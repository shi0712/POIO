# POIO 0.8.3

本版本修复 macOS Apple Silicon 客户端的首发问题：

- 启动动画会在主窗口就绪后可靠关闭，并带有超时兜底。
- Mumble 原生控制 API 在 macOS 可执行文件中显式导出，语音频道可以正常连接。
- macOS 构建会校验全部 POIO 原生符号。
- ModelScope 同时发布 DMG、在线更新 ZIP 与 `latest-mac.yml`。
